super + alt + f
	firefox

super + alt + {j,k,m}
	pamixer -{d 5,i 5,t}
	
control + alt + l
	slock

super + shift + g 
	$BROWSER -e $STANDUP_LINK

super + shift + l
	$TERMINAL -e lf

super + shift + v
	$TERMINAL -e sudo openvpn --config $HOME/client.ovpn

super + shift + m
	$TERMINAL -e neomutt
	
super + shift + s
	screenkey

super + shift + p
	$TERMINAL -e ncmpcpp

super + shift + n 
	$TERMINAL -e newsboat 
